# Changelog

记录 Painting-Box 的可观察变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 SemVer（0.x 阶段小改为 minor，微调为 patch）。

## [Unreleased]

### 功能（feat/template-iteration）
- **新增「2.35:1黑边」模板**：照片裁切为 2.35:1 宽银幕比例，输出 16:9 画布，上下自然黑边，底部黑边区域展示水印文字（机身、镜头、参数、日期等），还原电影画幅质感。
- **通用裁切功能**：新增 `cropRatio`（裁切比例）和 `cropPosition`（裁切位置）两个参数，支持将照片裁切为 2.35:1 / 16:9 / 4:3 / 3:2 / 1:1 等比例后再放入画布，裁切位置可通过 slider 自由调节（0=顶部，50=居中，100=底部）。裁切与画布比例正交独立，不影响现有模板行为。
- **版本号按钮简化**：移除检查更新刷新按钮（国内网络访问 GitHub API 不稳定易显示错误图标），版本徽章改为直接跳转 GitHub Releases 页面。

### 功能（feat/iteration-r3）
- **顶部栏品牌区显示版本号**：Painting Box 标题旁出现版本徽章 `v0.x.y`，自动检查 GitHub 最新 release（6 小时缓存）。检测到新版本时徽章高亮 + 下载图标，点击直达对应 release；常规状态点击进入 latest release 页查看更新历史。
- **手动检查更新按钮**：版本徽章右侧新增 RefreshCw 按钮，点击立即重新检查（绕过 6h 缓存）。检查中旋转动画，失败显示警告图标可重试。
- **GitHub 仓库快捷入口**：顶部栏新增 GitHub 图标按钮直达 https://github.com/Junglepan/Painting-Box；预览区上方同时显示完整社交矩阵：GitHub @Junglepan · 小红书 @土豆怪 · B站 @土豆怪725 · 抖音 @土豆怪。每个平台都是可点击外链跳转，非 GitHub 平台用品牌色字符标识。
- **客户端外观切换**：顶部栏新增主题切换器（亮色 / 暗色 / 跟随系统），完整暗色 token 套件应用全部组件（背景、卡片、阴影、选择环），系统切换实时跟随。配置持久化到 localStorage。
- **使用说明对话框**：顶部栏新增 HelpCircle 按钮，点击弹出按区域分组的中文说明（顶栏 / 照片列表 / 预览 / 参数面板 / 预设），覆盖每个按钮的用途。Esc 或点击窗口外可关闭。
- **Logo 尺寸独立扩展到 10-50**：从原 10-25 扩展，方便单独调大 logo 而不影响字号；字号保持 3-18。

### 修复（feat/iteration-r3）
- **MOCK/SAMPLE EXIF 默认地址纠正**：之前预览样图水印里的 `github.com/panbokui/painting-box`（错的）改为 `github.com/Junglepan/Painting-Box`，model 同步从 `@panbokui` 改为 `@Junglepan`。
- **Logo 增大时与水印对齐**：经典底栏 logo 改为底边基线对齐第一行 baseline，logo 放大时向上延伸，不再与下方第二行文字重叠。每行独立水平居中，logo + 第一行作为整体居中。

### 功能（feat/detail-iteration）
- **经典底栏照片立体感升级**：照片阴影从单层 `feDropShadow` 改为三层叠加（contact + ambient + far），分别对应"贴在画面上 / 给体积感 / 抬离纸面"三种光照层次。同一组用户控件（模糊/位移/强度）驱动三层按比例缩放，视觉上对齐 macOS Big Sur / Linear / Vercel 的卡片质感，等效透明度 ≈ 0.75 × opacity，与单层观感一致但层次感显著提升。
- **Logo 颜色自动对比度**：Logo 版本下拉新增「auto（跟随背景）」选项，按背景亮度自动选 black/white（仅当该 Logo 同时包含 black 和 white 变体时出现）。新建/重置默认值改为 `auto`，对老用户保持兼容。
- **文字颜色「自动」开关 + 手动颜色生效修复**：文字颜色行新增「自动」按钮（与画布方向 UI 一致）。点击「自动」恢复对比度策略；用色盘选色自动关闭 auto，手动颜色立即生效。修复了之前 autoTextContrast 永远开启导致用户选色不生效的 bug。
- **经典底栏模糊背景质量提升**：模糊背景在 `feGaussianBlur` 上添加 `color-interpolation-filters="sRGB"`（消除 linearRGB 边缘色偏），并叠加 5% fractalNoise 作为 dither 层（打散 8-bit 量化产生的色彩断层）；目标对齐 yiyin/Instagram 级模糊观感。

### 修复（feat/detail-iteration）
- **水印文字脱离 infoBar 漂浮**：照片高度小于可用区时，文字之前会浮在照片底部到画布底部之间的空白处。改为始终在 infoBar 内垂直居中，文字稳定锚定在水印栏上。
- **`minTopBottomMargin` 单位混乱**：垂直边距从基于 `canvasW` 改为基于 `canvasH`，符合用户对"上下边距百分比"的直觉；Rust 渲染计划同步对齐。
- **水印文字与照片/画布底部的最小留白**：`minInfoBarHeight` 保底从 `text + 12px` 提升到 `text + 32px`，文字上下至少各留 16px 设计稿留白，避免视觉拥挤。
- **内圆角时阴影丢失**：之前 clip-path 与 filter 同时挂在一个 `<g>` 上，SVG 规范导致 clip 把投影也裁掉。改为外层 `<g filter>` 包裹内层 `<g clip-path>`，两者分层互不干扰。
- **阴影黑色边框**：阴影矩形的 `fill="#000000"` 在子像素抗锯齿下渗出 1-2px 黑边。改为把 filter 直接挂到照片元素上，由照片的 alpha 直接投影，去掉独立黑色矩形。
- **magazine / photo-album 等模板照片消失**：照片元素引用了 `pb-photo-shadow` filter 但 `<defs>` 没输出（仅在 classic-bottom 单独输出过），切换模板后照片不可见。改为 `photoElement` 自包含 defs。
- **classic-bottom 自动对比度未生效**：`autoTextContrast` 在 SVG builder 里被硬编码为 false，黑色背景上仍用深色文字几乎不可见。改为读取 frameParams.autoTextContrast，并按背景颜色估算亮度。

### 重构（feat/detail-iteration）
- **死字段清理**：删除 `FrameParams` 与 Rust `ExportFrameParams` 中从未被任何渲染器读取的 5 类字段（`paddingTop/Right/Bottom/Left/Locked`、`outerRadius`、`textMargin`、`watermarkTopPadding`、`watermarkBottomPadding`），同步清理 store、setFrameParams、各模板默认值与测试 fixture。前后端共净删 77 行。

### 功能
- **模板库收敛到 12 款稳定模板**：移除「日期戳」「宝丽来」「极简白底」「瑞士网格」「柯达幻灯片」5 个模板入口、类型枚举、默认参数、预览分发与 SVG 分发；旧持久化配置或预设若指向已移除模板，会自动回落到「经典底栏」。
- **模板参数能力按模板释放并收窄范围**：新增模板级参数能力矩阵，每个模板只暴露自己能安全响应的参数，并为主图占比、边距、底栏高度、圆角、照片边框、字号、Logo 间距等控制项设置模板专属范围；剩余模板的默认参数和可调范围重新收敛，优先保证图片内容占比舒适、不越界、不拥挤。
- **全模板接入 SVG 单源预览/导出链路**：当前 12 款模板均可通过前端生成 SVG 模板，预览与导出共享同一份视觉描述；胶片齿孔、徕卡风、哈苏、富士经典保留独立 SVG builder，其余模板走通用 SVG 模板实现，并补齐高分辨率字体缩放与照片占位符导出路径。
- **Rust 导出端 magazine 渲染与字段双端对齐**：Rust 导出器新增杂志双栏（magazine）原生渲染——左栏 Logo + 型号/镜头，右栏焦距/光圈/快门/ISO，参数过长自动折两行，中间可选分割线，超长文本省略号截断；`show_date` / `date_format` / `custom_lines` / `photo_border_color` / `photo_border_style` 五个新字段现已在 Rust 侧生效，预览与导出完全对齐。同步更新了渲染计划几何对等检验（parity fixture）以匹配当前代码输出。
- **再添 5 款相机文化向经典模板**：模板库新增「富士经典」（fujifilm-classic，米白底 + 绿色 FUJIFILM 字标 + 胶片模拟标签 CLASSIC CHROME）、「哈苏」（hasselblad，纯黑底 + 橙色 HASSELBLAD 字标，极简到只剩型号）、「暗房样片」（darkroom-proof，黑色相纸边 + 米白照片纸 + 红色旋转 PROOF 印章 + 打字机风元数据）、「柯达幻灯片」（kodak-slide，米黄背景 + 幻灯片白色装裱框 + KODACHROME 64 红色色条 + 黄色描边）、「接触印样」（contact-sheet，黑底 + 上下白色齿孔条 + 白色 1px 边框 + Courier 字体的 FRAME 24A 编号）。模板库现共 **17 款**模板可选，覆盖品牌致敬、暗房文化、胶片美学、印刷流程四大风格谱系。
- **五款新经典模板上线**：模板库新增「小米 × Leica」（xiaomi-leica，白底双栏 + 红色竖线分隔 + 底部红色横线）、「老相册」（photo-album，奶黄色背景 + 四角暗三角装裱 + 草书风格题注）、「日期压印」（date-stamp，全幅照片 + 橙色 LCD 风格日期水印，模拟 90 年代傻瓜相机）、「瑞士网格」（swiss-grid，白底 + 照片下方细黑线 + 超粗型号字 + 右对齐参数）、「裁切标记」（crop-marks，白底 + 宽边距 + 四角 L 形裁切标记 + CMYK 色标条）。模板库现共 **12 款**模板可选。
- **Web 模板展示页 + GitHub Pages 自动部署**：Web 模式下默认进入纯展示页 `TemplateShowcase`，以网格（移动端 1 列 / 平板 2 列 / 桌面 3 列）渲染全部 7 款模板，每张卡片用同一张样图 + Painting Box 品牌 EXIF 渲染，便于横向对比视觉差异。新增 `.github/workflows/deploy-pages.yml` workflow：master 分支推送时自动 `bun run build` 并通过 `actions/deploy-pages@v4` 发布到 GitHub Pages（`VITE_BASE_PATH=/<repo>/` 自动注入）。需要在仓库 Settings → Pages 中将 Source 设为「GitHub Actions」即可一键启用。访问 `?app=1` 可在 web 下切回完整编辑器视图（用于 QA）。
- **Web 演示模式**：通过 `bun run dev` / `bun run build` 产出的静态包可直接部署到任何静态站（Vercel / Cloudflare Pages / GitHub Pages）。Web 端检测到非 Tauri 环境后自动启用「演示模式」：以默认样图 + Painting Box 品牌 EXIF 渲染所有 7 款模板，可切换模板与参数实时查看效果；导入照片 / 导出图片 / 拖拽 / 打开目录等桌面专属功能在 web 下被守卫，header 右上角显示「演示模式 · 仅预览」标识。Logo SVG 在 web 下走 `public/brand-logos/` fetch 加载，无需 Rust 命令。
- **三款新模板上线**：模板库新增「极简白底」（minimal-fullbleed，照片四周等宽白边 + 底部极小信息）、「电影黑边」（cinematic，上下黑边 letterbox + 下黑栏白色文字）、「胶片齿孔」（film-strip，上下窄黑带 + 左右程序绘制的齿孔图案 + 右下脚参数与日期），模板库现共 **7 款**模板可选。
- **自定义文字行**：水印支持最多 2 行用户自定义文字（每行 32 字以内），可与 EXIF 信息并存。经典/宝丽来/杂志双栏模板均可用；杂志模板下自定义文字进入左栏，其他模板追加在末尾。
- **拍摄日期显示**：水印显示项新增「日期」开关，开启后基于 EXIF DateTimeOriginal 渲染拍摄日期；日期格式可在「内容」分组中选择（`YYYY-MM-DD` / `YYYY/MM/DD` / `YYYY.MM.DD` / `DD MMM YYYY` / `MMM DD, YYYY`）。杂志模板下日期进入右栏（与参数同列），其他模板追加为单独一行。
- **照片边框样式**：在「照片」分组下，当边框宽度 > 0 时新增「样式」（无 / 实线 / 虚线）和「颜色」选项，原本固定白色的照片边框现在可以自定义颜色或采用虚线。
- **参数面板分层**：参数面板顶部保留常用项（布局 / 背景 / 显示项 / 内容），把「阴影 / 照片 / 文字 / Logo / 分隔线」收进可折叠的「高级」分组，新手默认看到的项更聚焦。
- **新增「杂志双栏」模板**：底栏左侧显示 Logo + 相机型号/镜头（左对齐），右侧显示拍摄参数（焦距、光圈、快门、ISO，右对齐）；参数单行显示不下时自动换为两行；中间可选竖向分割线；超长文本自动省略号截断。模板基础参数为白底、无圆角、80px 底栏高度、轻阴影。
- **激活「宝丽来」模板**：之前已存在但隐藏的宝丽来模板正式开放，模板库中现共 **4 款**模板可选（经典底栏 / 宝丽来 / 杂志双栏 / 极简角标）。
- **内置字体扩展**：新增 Playfair Display（英文衬线）、Bebas Neue（英文超粗无衬线）、Noto Sans SC（中文无衬线，GB2312 subset，6884 字）三款字体，均内置于应用二进制，跨平台一致渲染，无需依赖系统字体。字体选择器改为按「英文」/「中文」分组展示，共 6 款可选字体（英文 4 款 + 中文 2 款）。

### 重构
- **SVG 模板注册表成为导出入口**：`buildWatermarkSvgTemplate` 统一管理模板到 SVG builder 的分发，照片列表单张/批量导出都会携带 `svgTemplate`，Rust 端收到 SVG 后通过 `resvg` 渲染，旧 Canvas/Rust 手写模板保留为对照与回退。
- **模板元数据集中到注册表**：新增 `src/lib/watermark/template-registry.ts` 作为模板配置单一真相源（包含 `mode/placement/exposedInLibrary/displayFields/configLocks/liftLogoOnly`），`TEMPLATE_LIBRARY`、`getTemplateLayout`、`getTemplateDisplayFields`、`applyTemplateConfigConstraints`、`shouldLiftLogoOnlyWatermark` 全部改为派生自 registry。新增模板的成本由"散改 4 处文件"降为"在 registry 加一条"。前端渲染算法与 Rust 导出端零改动，像素输出保持完全一致。
- **预览渲染器分发**：`PreviewPane` 改为按 `templateKind` 分发到不同的渲染函数（`drawMagazinePreview` 走双栏、其他模板走 `drawClassicBottomPreview`），为后续模板独立渲染器架构铺路。

### 修复
- **Windows 导出字体、JPEG 质量与模板栏滚轮修复**：SVG 导出端不再依赖系统字体，改为和预览统一优先使用内置字体 family；SVG 原图占位符对 JPEG/PNG 保留原始字节，避免中间二次有损压缩，最终 JPEG 导出改为 4:4:4 无色度抽样；模板库和预设栏支持鼠标滚轮横向滚动。
- **经典底栏竖图横画布主图占比修复**：经典底栏在竖图放入横画布时，不再让高度约束完全吃掉主图占比滑条；竖图横画布会在等比适配后继续按主图占比缩放，避免照片贴到上下边界且保证占比调节有实际反馈。该修复位于共享 `buildPreviewRenderPlan`，Canvas 预览与 SVG 导出同步生效。
- **竖图水印形态跟随原图横竖**：哈苏、富士经典、杂志双栏在竖图放入横画布等场景下，按原图竖图形态排水印；品牌/机身从画布左安全边开始，参数保持同一行并锚到画布右安全边，避免此前按窄主图堆叠导致水印超出底部边界。
- **接触印样固定模板与画面占比提升**：接触印样改为固定 FRAME 模板，Logo、机身、镜头、参数、日期显示项全部锁定关闭；默认主图占比提升到 90，边距降到 3，底栏降到 72，并收窄可调范围。SVG 同步降低齿孔高度和字幕预留，放大照片内容展示，保留 FRAME 编号且不输出 EXIF 文本。
- **暗房样片固定模板与画面占比提升**：暗房样片改为固定 PROOF 模板，Logo、机身、镜头、参数、日期显示项全部锁定关闭；默认主图占比提升到 88，边距降到 2，底栏降到 80，并收窄可调范围。SVG 同步减少黑边/纸张内边距和底部预留，放大照片内容展示，同时保留 PROOF 印章且不输出 EXIF 文本。
- **哈苏默认画面占比提升**：哈苏按富士经典同样策略收敛主图占比与边距参数，主图占比范围调整为 85-95、默认 90，边距默认降为 0、底栏默认降为 80；SVG 几何同步降低硬编码边距与底栏预留，并确保主图占比调节会真实改变照片尺寸、水印下移后仍不超出画布。
- **富士经典默认画面占比提升**：富士经典主图占比范围调整为 85-95，默认 90；边距默认降为 0，底栏默认降为 80 且下界降到 64。SVG 几何同步降低硬编码边距/底栏下界，并改为只按宽度应用主图占比，让照片占用更多高度，同时测试约束水印仍在画布内。
- **旧相册收敛为纯照片装裱模板**：旧相册锁定 Logo、机身、镜头、参数、日期全部关闭，显示项面板保留完整入口但置灰不可编辑；默认主图占比提升到 90，边距降到 2，底部文字区归零，SVG 不再预留或渲染文字区域，让照片展示面积明显增大。
- **显示项配置统一为完整展示 + 禁用态**：显示项面板现在保留 Logo、机身、镜头、参数、日期的完整入口；模板不支持或被锁定的显示项会置灰禁用，而不是从 UI 中消失。胶片齿孔同步锁定 Logo 与镜头为关闭，旧预设也不能重新打开。
- **胶片齿孔默认参数收敛**：胶片齿孔默认保持不显示 Logo 与镜头，主图占比默认从 88 提升到 90，并将可调范围调整为 85-95，提升默认照片展示面积且保留可调空间。
- **电影黑边默认主图占比提升**：收窄默认上黑边和底部水印栏，默认底栏高度从 104 降到 84，并同步调整可配置范围；3:2 横图在电影黑边模板中的默认展示宽高占比更大，同时仍保留上下黑边语义。
- **极简角标默认视觉微调**：默认 Logo 尺寸从 12 提升到 14，并增加 Logo 与图片之间的垂直距离；SVG 渲染中 Logo 尺寸不再被小字号上限压住，确保默认效果实际变大且仍保持在底部水印区内。
- **极简角标 SVG 位置修正**：极简角标在 SVG 预览/导出链路中重新预留底部水印区，Logo 或兜底文字按主图右边缘对齐显示在图片下方，不再覆盖到照片内容上。
- **杂志双栏显示项与布局收敛**：杂志双栏现在只支持 Logo、机身、参数三项显示；Logo 与机身固定在左侧，参数固定在右侧，镜头、日期和自定义文字不再参与该模板输出。主图占比范围收敛为 85-95，默认 85，并将底部水印整体下移以增加与图片的距离。
- **经典底栏默认参数与圆角适配**：经典底栏默认字体改为 PingFang SC，默认 Logo 尺寸改为 20，主图占比保持 85，默认边距、底栏高度和内圆角归零；SVG 主图现在会在设置内圆角时使用 `clipPath` 裁剪真实照片，修复只有边框圆角生效、图片本体仍直角的问题。
- **经典底栏阴影配置入口恢复**：参数面板新增顶层「阴影」分组，经典底栏重新开放照片阴影开关、模糊、下移和强度配置，并通过模板能力矩阵限制范围，避免无效参数暴露到不支持阴影的模板。
- **SVG 链路 Logo 与原图导出修复**：预览和导出现在会把选中的 Logo SVG 注入到模板资源中，修复 SVG 模板下 Logo 不显示的问题；Rust SVG 导出改为通过 `usvg` 图片 resolver 解析 `__FUJI_PHOTO__` 原图占位符，不再把整张原图替换成超长 data URL，避免导出结果只剩水印、照片消失。
- **SVG 参数预览拖动闪烁修复**：SVG 预览从直接替换 `dangerouslySetInnerHTML` 改为 Blob URL 双缓冲显示，新 SVG 帧先通过 `Image` 预加载，加载完成后再替换当前画面；拖动参数时保留上一帧，避免照片 `<image>` 节点被销毁重建造成短暂空白。
- **经典底栏 SVG 对齐旧视觉契约**：经典底栏 SVG 复用旧 `buildPreviewRenderPlan` 几何计算，并修复高分辨率下字号、底栏高度和水印块位置缩放，保持原经典底栏效果的同时接入 SVG 导出策略。

## [0.1.0] - 2026-04-25

### 功能
- **照片独立配置**：每张照片现在可以单独保存自己的 `TemplateConfig` 和 `FrameParams`，互不干扰。选中照片时参数面板读写该照片的独立配置（`null` = 继承全局模板）；未选中任何照片时编辑全局默认。预设应用、重置操作也分别针对当前照片或全局生效。有独立配置的照片在列表中显示"独立配置"标签。导出时自动使用各照片自己的参数。
- **导出目录独立展示**：照片列表工具栏下方新增常驻导出目录栏（文件夹图标 + 路径 + 选择/更改/清除）；批量导出弹窗移除目录选择，专注格式/质量/冲突策略。首次批量导出选择目录后自动保存为默认，后续无需再选。目录全局持久化，重启保留。
- **动态导出队列（Plan B）**：单张导出不再阻塞其他照片。设置"默认导出目录"后，点击任意照片的导出按钮即自动生成路径、加入队列，500ms 防抖后批量并行发送至 Rust；未设置目录时仍弹 Save 对话框。每张照片的导出按钮独立显示状态（闲置 / 排队旋转 / 已导出），不受其他照片影响。默认目录持久化保存，重启后保留。"批量导出"面板新增目录选择/清除入口，设置后整列导出同样跳过弹窗。
- **水印开关**：显示项新增"水印"主开关；关闭时导出无水印区域，图片撑满画布（infoBarHeight=0）；开启时恢复完整水印布局；Logo 仅在水印开启时渲染。
- **批量导出并行化**：批量导出从顺序 `for-await` 改为一次调用 `exportBatchPhotos`（rayon 并行），所有图片同时处理；导出开始后面板立即关闭，进度通过事件推送更新，UI 全程不阻塞；单张导出也取消了阻塞守门。
- **参数范围收敛**：所有可输入数字的控件（slider + 数字输入框）统一做范围校验，输入超出范围时即时 clamp 到边界，不再等到失焦才生效；具体范围：Logo 尺寸 10–25、Logo 间距 0–30（默认 10）、内圆角 0–50（默认 0）、主图占比 70–95（默认 85，所有模板统一）。
- **画布比例固定化 + 横竖图切换**：导出图片的比例不再受源图尺寸影响，仅由所选画布比例决定；源图始终等比缩放居中，不裁切不变形。新增横图 / 竖图切换按钮（`⬜横` / `⬜竖`），点击后所有比例同步翻转（如 3:2 ↔ 2:3），预览与 Rust 导出双端一致。
- **画布比例列表整理**：移除"原图（auto）"和 2.39:1 选项，比例全部规范化为横图基准（宽≥高），共 8 项；首位改为"原图"标签（对应 3:2，竖图模式下保持"原图"语义标签不变）。

### 修复
- **内置字体支持 + 导出字体消失修复**：新增 Inter 字体内置通道（`src-tauri/fonts/` 放入 TTF 后自动编译进二进制，无需依赖系统字体）；修复 `font_paths` 在所有映射路径失败时不回退 Arial 的 bug，确保任何系统上导出都能渲染文字；默认字体改为 `inter`（内置时优先），前端同步加 `@font-face`；PingFang 路径不可靠时自动降级到 Arial/ArialHB。
- **阴影扩散范围修正**：Rust 阴影 `pad = blur * 2`，使盒模糊尾部有足够空间，避免截断成矩形边缘，视觉上与 Canvas shadowBlur 更接近。
- **导出端几何缩放对齐预览基准**：Rust 导出修复了 `infoBarHeight`、`innerRadius`、`photoBorder` 在高分辨率图片下未按 `resolution_scale` 缩放的问题，水印区域与照片边界在预览/导出间保持一致。
- **Logo 缩放与绘制路径一致化**：导出端首行 Logo 改为先按排版计算尺寸缩放后再叠加，避免“排版按小尺寸、实际按原图尺寸绘制”导致的 Logo 偏大。
- **字体度量对齐**：Canvas 预览文本度量改为优先使用 `fontBoundingBoxAscent/Descent`，减小与 Rust `ab_glyph` 字体指标差异造成的基线偏移。
- **跨端字体映射统一**：新增共享字体映射 `src/shared/font-mapping.json`，前端预览与 Rust 导出统一从同一映射源解析字体族与平台路径，降低字体 fallback 不一致导致的观感漂移。
- **Rust 测试参数结构同步**：`canvas_orientation` 新字段已补到相关渲染测试与 smoke test 构造体，避免测试因结构体字段缺失而编译失败。
- **预览/导出共享水印版式常量**：新增 `src/shared/watermark-layout-spec.json`，预览 Canvas 与 Rust 导出统一读取同一套字重比例、行距、Logo 比例、角标边距与可读性阈值，减少双端漂移。
- **Rust 导出接入共享 Logo Catalog**：新增 `src/shared/logo-catalog.json` 并由前端/导出共用；Rust 不再只靠文件名猜测，改为按 catalog 与统一 fallback 规则解析 Logo 资源。
- **新增内部水印模板 DSL（无 UI 暴露）**：`TemplateConfig` 新增 `watermarkTemplate?: string[]`，支持 `{Make}`、`{Model}`、`{LensModel}`、`{FocalLength}`、`{FNumber}`、`{ExposureTime}`、`{ISO}`、`{Params}` 占位符；未配置时保持原显示项逻辑。
- **文字版式改为字体度量驱动**：预览与导出都改为基于 ascent/descent/line-height 计算文本块高度与基线位置，弱化固定比例常量在不同字体下的偏差。
- **新增自动可读性策略（内部开关）**：`FrameParams` 新增 `autoTextContrast`（默认开启）；根据水印区域亮度自动切换浅/深文字与分割线颜色，暗图/亮图下可读性更稳定。
- **`minimal-corner` 导出横向位置对齐预览**：Rust 导出端不再把底部水印模板统一居中；`minimal-corner` 现在会和预览一样按图片右边缘内缩定位，避免导出水印跑到画面中间。
- **移除水印顶距/底距控制**：布局区不再暴露这两个调节项，经典模板的水印块恢复为图片区底边与画布底边之间默认居中定位，不再允许手动上下偏移。
- **水印顶距/底距改为像素直调**：两项参数从百分比改为像素单位，`水印顶距` 范围为 `0–120px`，`水印底距` 范围为 `0–240px`，默认值同步提升为更可见的像素量，拖动时能直接看到整块水印上下移动。
- **水印上下位移滑条范围放大**：`水印顶距` 调整为 `0–12%`，`水印底距` 调整为 `0–24%`，并把步进收成 `1%`，给整体水印上移留出更明显的可调空间。
- **水印顶距/底距改为直接控制整体上下位移**：经典模板的“水印顶距”和“水印底距”不再只是收紧可用边界，而是直接驱动整块水印文字在图片区底边到画布底边之间上下平移；同时保留边界钳制，避免文字撞图或超出画布。
- **水印顶距/底距位移基准改为画布高度**：为避免 `0–5 / 5–15` 区间在底栏内位移过小导致“看不出变化”，两项边距改为按画布总高度换算后再应用到水印区约束，拖动反馈更直观。
- **水印顶距/底距灵敏度修正**：两项边距现在按“照片底边到画布底边”的可用区间计算百分比，而不是按底栏高度换算，拖动时对水印整体上下位置的影响更明显。
- **水印底距改为直接驱动垂直位置**：经典模板的“水印底距”不再仅作为边界约束，而是直接参与居中区域计算；增大底距会稳定地将水印整体上推，预览与导出一致。
- **分割线位置改为图片与水印之间**：经典模板开启分割线时，线条不再固定贴在水印顶部，而是动态绘制在“图片底边与水印块起始位置”之间，语义与视觉更一致。
- **水印垂直基准改为图片区底边到画布底边区间居中**：经典模板的水印块默认不再偏向底部，而是以“照片底边 ~ 画布底边”区间中线为基准定位；预览与导出一致。
- **Logo 与文字底边视觉对齐修正**：经典底栏的 Logo 对齐基线时改为基于“非透明有效区域”计算，自动裁掉 SVG 透明留白后再参与排版，避免看起来底边仍不齐；预览与 Rust 导出一致。
- **EXIF 复制按钮状态收敛**：图片列表中“复制 EXIF”按钮仅在该照片 EXIF 解析成功且有有效信息时可点击；未解析/解析失败时按钮置灰禁用，并显示“无信息”标识。
- **Logo 改为严格基线对齐**：经典底栏首行 Logo 取消光学偏移，改为与文字基线严格重合；预览与 Rust 导出一致。
- **Logo 与字号改为联动缩放**：经典底栏首行 Logo 的实际渲染高度现在会随“字号”同步缩放（同时保留 Logo 尺寸滑条语义），避免调大/调小文字时 Logo 视觉比例不跟随；预览与 Rust 导出一致。
- **经典底栏布局控件收敛**：参数面板移除“底栏”和“文本间距”滑条；文本行距改为模板内固定值（默认 `0` 语义，不再作为可调项），避免无效调参干扰布局。
- **新增水印上下边距双控制**：布局区新增“水印顶距（距照片）”与“水印底距（距画布底）”两个滑条（`0–20%`），用于独立控制水印整体在底栏中的上下留白；默认值同步上调，解决水印过贴底边的问题，预览与导出一致。
- **移除水印区灰色叠层**：经典底栏不再对底栏区域额外叠加半透明灰层，恢复为纯底色背景；预览 Canvas 与 Rust 导出保持一致。
- **新增“水印底距”微调滑条**：参数面板新增“水印到底边距离”调节（`0–2%`，步进 `0.05%`），默认 `0.3%`；该参数仅影响底栏内水印块的垂直位置，不影响图片区布局边界。
- **参数面板移除“信息位置”控件**：信息位置改为模板策略维度后，不再作为用户可调项暴露；面板中已移除该控件，底层字段暂保留以兼容历史本地配置。
- **底栏增加默认可见层次**：经典底栏在分界线关闭时新增轻微底色叠层（浅底用低透明深色，黑底用低透明浅色），让“水印区”有稳定视觉存在感，同时不改变默认分界线开关语义。
- **文本间距影响范围收敛并扩大内圆角范围**：文本间距滑条范围收紧为 `0–1`（步进 `0.05`），并将底栏最低高度约束改为固定缓冲，避免文本间距继续牵动图片区边界；同时图片内圆角滑条范围提升为 `0–200`。
- **经典底栏改为图片区与水印区硬分离**：经典模板布局调整为“上方图片区 + 下方水印区”解耦模型，图片区参数不再挤占底栏区域；分界线默认保持关闭，仅在手动开启“底栏显示”时绘制。
- **预览画布移除 DOM 圆角并提升阴影默认浓度**：预览画布元素去掉 `rounded` 样式，避免外层样式掩盖“画布圆角=0”的效果；同时模板默认图片阴影浓度提升为 `100%`，并把阴影浓度滑条上限同步放宽到 `100`。
- **模板默认画布圆角设为 0**：模板基础参数中的 `outerRadius` 默认值调整为 `0`，与当前矩形底片语义保持一致。
- **滑条轨道长度统一**：参数面板 Slider 的单位区域改为固定占位（无单位时也保留空位），解决“主图占比”因 `%` 单位导致轨道看起来更短的问题，保持各行滑条长度一致。
- **布局参数范围与默认值调整**：内圆角滑条范围改为 `0–100`，主图占比滑条范围收敛为 `70–85`（默认同步收敛到 `85`）；上下边距保持默认 `2.4%`，并从参数面板中移除该调节控件。
- **经典底栏默认布局收敛**：为减少“画面像被分成两层”的观感，默认参数调整为更轻底栏与更聚焦主图（`infoBarHeight: 72`、`mainImageWidthRatio: 85`、`minTopBottomMargin: 2.4`、`textMargin: 0.25`）。
- **参数面板移除“外圆角”控件**：经典底栏画布已改为矩形底片后，`外圆角` 对当前模板不再生效；面板中同步移除该控件以避免误导。底层字段暂保留兼容历史配置与预设。
- **提高默认图片阴影可见度并移除画布圆角**：模板基础配置中的阴影透明度默认值提升到 `24%`，让主图阴影更易感知；同时经典底栏预览去除画布外框圆角，保持底片矩形语义与最终导出一致。
- **阴影偏移改为按图片高度百分比配置**：阴影偏移值语义从固定像素改为“主图高度百分比”，默认值调整为 `0.06%`；预览与导出两端同步生效，并兼容历史像素配置避免本地旧参数突变。
- **模板基础配置默认开启 Logo**：模板基线配置中的 `showLogo` 由默认关闭调整为默认开启；切换模板或回到模板基础配置时会直接带出 Logo，减少每次手动打开的操作。
- **Nikon Logo 默认版本调整为 black**：在 `auto` 跟随品牌模式下，若版本仍为默认值 `original`，Nikon 现在会默认落到 `black` 资源；手动选择其它 variant 时仍保持用户选择，预览与导出一致。
- **经典底栏水印改为基线对齐**：预览 Canvas 与 Rust 导出中的首行 Logo+文字垂直对齐从“视觉居中”调整为“文字基线对齐”，默认观感更稳，且双端渲染保持一致。
- **Nikon Z 机身显示规则更新**：Nikon 型号格式改为 `Z 7` / `Z 7II` 风格（`Z` 与主型号保留空格，主型号保持阿拉伯数字，迭代号转罗马数字且与主型号紧贴），并补充 `Z fc` / `Z f` 字母机型归一化；预览与 Rust 导出两端同步一致。
- **经典底栏移除文字对齐配置**：参数面板“文字”分组中删除对齐选项，经典底栏模板固定使用居中水印布局，避免与后续模板库按模板定义位置策略冲突。
- **Logo 尺寸控件改回小范围**：Logo 尺寸默认值调整为 `20`，滑条范围改为 `10–30`，更贴近当前模板观感和实际可控区间。
- **预览 Logo 尺寸不再随照片分辨率跳变**：预览里的 Logo 高度已和源图宽度脱钩，切换不同像素尺寸的照片时，同一套参数下 Logo 不会再忽大忽小。
- **文字配置收紧为字体 + 字号**：移除字重、字距、行高调节，水印文字固定使用粗体；新增字体选择，默认 `PingFang SC`，并在 Rust 导出端同步接入该字体选择。
- **`auto` 模式下版本选择不再被覆盖**：Logo 品牌为 `auto` 时仍然跟随照片品牌，但手动选择的 Logo 版本现在会被保留；只有当前品牌不支持该 variant 时才回退到推荐版本。
- **`跟随照片` 模式下版本列表改为动态可选**：Logo 品牌保持 `auto = 跟随照片` 语义不变，但版本列表现在会基于当前照片映射出的真实品牌动态展示，不再因为 `auto` 而失去版本选择能力。
- **Logo 尺寸范围上调**：Logo 尺寸滑条范围从 `60–100` 提升到 `80–120`，和当前视觉基准更匹配。
- **Logo 控件范围收紧到有效区间**：Logo 尺寸滑条改为 `60–100`，默认值提升到 `80`；Logo 间距滑条改为 `40–80`，默认值提升到 `40`，避免低区间几乎无可见变化的无效调节。
- **Logo 视觉尺寸上调**：Logo 区虽然已有尺寸滑条，但同数值下相对首行文字仍偏小；经典底栏的预览与 Rust 导出现在统一提升 Logo 实际渲染比例，让当前尺寸值显示更接近文字权重。
- **模板显示项移除时间与 GPS**：经典底栏模板不再支持把时间或 GPS 加入水印文案；显示项开关里同步移除这两项，水印只保留 Logo / 机身 / 镜头 / 参数。
- **Nikon 型号前缀去重修正**：`NIKON CORPORATION` 这类 EXIF 厂商值下，Nikon 型号归一化现在会先按标准化品牌名剥离前缀，避免第一行文案继续出现 `NIKON ℤ 7 II` 这类重复品牌。
- **品牌名默认不再进入水印文案**：经典底栏第一行默认只显示按品牌规则归一化后的相机型号，品牌信息仅用于 Logo 映射，避免 Logo 与品牌文字重复出现。
- **Logo 自动选择与预览多行布局修正**：未手动指定 Logo 品牌时，预览/导出会优先选择该品牌的 `icon-*` 版本，避免 Logo 与品牌文字重复；同时修复经典底栏在居中/右对齐下未推进行高导致的多行文案重叠问题。
- **Phase2 smoke test 对齐 async 命令实现**：`src-tauri/tests/phase2_smoke.rs` 改为通过 `block_on(async { ... })` 调用 `load_photos / load_photo_preview / export_single_photo`，并同步补齐当前导出参数结构，避免测试继续按旧同步接口编译失败。
- **品牌映射与 Logo 资源源头收紧**：品牌名称格式化规则对齐 `yiyin`（Nikon `Z -> ℤ`、`_N -> 罗马数字`，Sony `ILCE- -> α`，统一去除 `CORPORATION`）；同时新增 `logoKey -> asset` 映射层，并将 `Camera-Logos-SVG` 作为新的主 Logo 资源来源纳入仓库，供后续模板渲染复用。
- **品牌 Logo 全量归档并统一命名**：`Camera-Logos-SVG` 的全部 SVG 已收录到 `public/brand-logos`，统一整理为 `key-variant.svg` 命名；同时新增显式 `logo-catalog`，后续代码只依赖 catalog 查找，不再隐式猜文件名。
- **左侧 EXIF 改为原始信息展示**：参数区 EXIF 面板不再套用品牌/型号格式化逻辑，机身与曝光参数改为按解析出的原始值展示；模板预览与导出继续使用独立的文案格式化函数。
- **预设区新增入口补入锁定域**：照片解析期间，预设区改为整块统一禁用，包含“新增预设”在内的所有入口都会随整体预览状态锁定，避免半成品状态被保存为预设。
- **模板库与预设区跟随预览加载锁定**：当前选中照片整体预览未 ready 时，底部模板区与预设区同步进入不可编辑状态，避免生成中的照片被切模板或套用预设导致预览语义漂移。
- **预览与 EXIF 改为真正后台并发**：Tauri 命令从同步重任务切到 `spawn_blocking` 后台线程池，点击照片后预览生成和 EXIF 解析不再占住前台交互线程，减少“点一下就卡住”的体感。
- **参数区改为按“整体预览 ready”解锁**：当前选中照片只有在预览与 EXIF 都完成后才允许调整参数；加载期间预览区与 EXIF 区统一显示 `soon...`，避免半成品预览与参数修改相互打架。
- **预览加载逻辑回到按需触发**：导入后只展示照片列表，不做后台预取；点击某张照片后才开始解析该张预览与 EXIF，未就绪时预览区显示 `soon...` 过渡态，加载完成后自动替换为真实预览。
- **点击照片不再触发后台解析重排**：预览与 EXIF 队列改为独立于选中态运行，点击照片只切换展示状态；后台任务按导入顺序低优先级推进，未就绪时由预览区过渡动画承接。
- **`原图` 比例默认回落为 `3:2`**：`canvasRatio = auto` 不再直接跟随源文件实际宽高比，经典模板默认以 `3:2` 作为稳定画布基线，避免不同来源比例导致模板观感波动。
- **原图比例下主图占比不再改动画布**：`canvasRatio = auto` 改为先锁定固定画布比例，再在固定画布内缩放/居中主图；调整主图占比时只改变照片展示，不再把整个画布高度一起拉伸。
- **品牌名标准化**：新增 `src/lib/exif/brand.ts` / `brand.rs`；Sony `ILCE-7M4` → `α 7m4`；Nikon `NIKON Z 6_2` → `ℤ 6 II`（Z→ℤ，`_N` 后缀转罗马数字）；其余品牌首字母大写其余小写并剥离 CORPORATION；Canon/Fujifilm 等默认小写并去除型号中重复的品牌前缀；参数行 ISO 格式改为 `ISO100`（无空格）；参数面板"机身"只读行同步走标准化路径。
- **Rust 文字渲染：font8x8 → ab_glyph 系统字体**：导出图片文字改为抗锯齿矢量渲染（macOS 使用 Arial/Arial Bold，Windows 使用 arialbd.ttf，Linux 使用 DejaVu Sans）；Cargo.toml 移除 `font8x8`，加入 `ab_glyph = "0.2"`。
- **导出质量参数接线**：`ExportSinglePhotoRequest` 新增 `exportQuality` 字段（60-100），前端 popover 质量滑条值传入请求，Rust JPEG 编码器改用动态质量（之前固定 92）。
- **Canvas 与 Rust 渲染对齐**：Canvas `classic-bottom` 去掉第一行 italic，统一用 `Arial` 字体和 700 字重；参数分隔符从 ` · ` 改为 `  ·  `（与 Rust 侧一致）。
- **Clippy 修复**：修复 exif 模块冗余 `as u32` 转换；将 `.max(1).min(40)` 改写为 `.clamp(1, 40)`；修复 `parse_color` 两分支相同的 if-else。

### 修复（续）
- **字号缩放对齐**：Canvas 预览移除错误的 `* scale` 乘数（Canvas 始终 900px，无需缩放）；Rust 导出新增 `resolution_scale = frame_w / 900.0`，全分辨率导出字体随图片尺寸线性放大。
- **默认字号调整为 22**：对应 900px 画布宽度下约 26px 主字号（≈ 2.9% 宽，与主流参考风格一致）；字号滑条范围更新为 12–60。

### 功能
- **`polaroid` 与 `minimal-corner` 接入初版样式**：模板库中的 `polaroid` 与 `minimal-corner` 不再只是缩略图占位；前者采用更大的底部白边和更稀疏的文字信息，后者改为真正的右下角轻量角标，预览与 Rust 导出同步生效。
- **模板库新增“经典白底栏”**：新增第二套可切换模板（`classic-white`），提供独立基础参数与缩略图，支持与经典底栏一样的实时预览和导出链路。
- **图片列表新增 EXIF 一键复制**：每张照片条目新增“复制 EXIF”按钮；优先复制已解析数据，未解析时会先读取再复制到剪贴板，便于快速分享机身/镜头/参数信息。
- **真实品牌 Logo 渲染接入预览与导出**：经典模板不再使用占位 Logo 文案，预览 Canvas 与 Rust 导出都改为读取 `brand-logos` 中的真实 SVG 资源；同时参数区 Logo 小节新增全局 `logoKey / logoVariant` 选择器，并按当前品牌展示该品牌全部实际可用 variant。
- **底栏文案改为动态多行布局**：经典模板底栏不再把镜头、参数、时间挤进两行；现在会按“机身 / 镜头 / 参数 / 时间”顺序按需逐行显示，并为可变行数预留额外行距，避免开启字段后互相挤压。
- **镜头与时间展示层清洗引号**：底栏展示会在渲染前清除镜头与时间字符串的包裹引号，不影响左侧 EXIF 原始信息保留。
- **字体范围与默认间距收紧**：字号滑条范围收紧为 `16–34`，默认字号从 `22` 收到 `20`；同时把主图与水印区的默认最小垂直间距上调，减少文字顶住图片的情况。
- **自动预览默认关闭且配置持久化**：`自动预览` 开关默认不启用，并持久化到本地；模板参数、显示项与当前模板选择也同步持久化，重启后自动恢复上次配置。
- **照片列表新增自动预览开关与批量解析按钮**：支持切换“导入后自动解析预览/EXIF”模式；关闭时导入仅展示列表，开启时新导入照片自动入后台解析队列；同时新增“解析所有照片”按钮，可将当前未解析项批量入队。
- **批量解析入队即显示中间态**：点击“解析所有照片”后，当前待解析照片会立刻被标记为排队中；此时这些照片不再被视为“可再解析”，按钮会随之禁用直到队列消化完成。
- **照片列表状态改为图标标识**：`可预览 / 导出` 两个状态从文字标签收敛为图标徽标，并保留悬浮提示，降低列表视觉噪音同时保留状态可读性。
- **照片列表新增双状态标签**：每张照片条目显示 `可预览` 与 `导出` 两个状态，分别反映整体预览是否 ready，以及最近一次导出结果，便于快速判断当前处理进度。
- **导入改为前端即时入列**：拖拽和批量选择后，照片名称先立即进入列表，不再等待后端遍历路径返回；后台再异步补全预览与 EXIF，减少导入时的等待感。
- **预览缩略图改为 JPEG 传输**：后台预览生成从 `PNG base64` 切到更轻的 `JPEG base64`（质量 84），降低 Rust 编码、前端传输和浏览器解码成本，改善批量导入后的预览流畅度。
- **导入后后台限流预取照片信息**：照片导入完成后即开始后台并行但限流加载预览与 EXIF（优先当前选中项），避免状态更新导致一次性打满所有解析任务，交互保持可继续操作。
- **画布比例补充 `16:10`**：参数面板比例选择器新增 `16:10`，与现有标准比例一起驱动预览和导出共用的画布约束逻辑。
- **新增画布比例选择器**：参数面板布局区加入常用标准比例切换（含 `1:1 / 4:5 / 3:2 / 4:3 / 5:4 / 16:10 / 16:9 / 20:9 / 21:9 / 2.35:1 / 2.39:1`），驱动预览与导出共用的画布约束逻辑。
- **经典模板切换为约束式参数模型**：布局不再依赖自由四边距，改为主图占比（`mainImageWidthRatio`）、最小上下边距（`minTopBottomMargin`）、文本间距（`textMargin`）三项约束，降低可调参数导致的失真。
- **移除照片缩放调节**：参数面板去除“照片缩放”滑条，预览与导出统一固定主图按画布宽度渲染，避免自由缩放导致构图失衡。
- **照片列表支持单项删除**：右侧名称列表新增删除按钮，移除后即时从导出数据源剔除，后续导出不再包含该照片。
- **导入后不自动选中照片**：首次/批量导入仅写入列表，不触发默认选中，避免导入瞬间连带预览与元数据任务造成阻塞体感。
- **选中后并行加载预览与 EXIF**：取消“先预览后 EXIF”的串行依赖，改为并发触发两条按需链路，减少单图就绪总等待时间。
- **导入阶段改为零处理**：拖拽/选择导入时只登记照片路径与 id，不再同步生成缩略图/读取尺寸/解析 EXIF；预览与 EXIF 均改为选中照片后按需加载。
- **导入按钮容错增强**：文件选择器调用增加异常捕获，权限或系统调用失败时在列表区显示明确错误，不再静默失败。
- **EXIF 改为按需懒加载**：导入阶段不再同步解析 EXIF，只返回路径/尺寸/缩略图；选中照片时再读取 EXIF 并展示在参数面板，导出若未加载则 Rust 端自动兜底解析。
- **批量导出按钮**（UI 框架）：`PhotoList` 头部加入主色 Download 按钮，点击弹出设置 popover（格式 JPG/PNG/WebP、质量 60-100、输出目录、开始导出）；无照片时禁用。导出动作占位，待 Rust 后端接入。
- **移除 `AppHeader` 导出按钮**：导出职责下沉到照片列表工具栏，header 只保留"设置"。
- **导入真实照片链路启动**：支持原生文件选择和系统文件拖拽导入，列表数据由 Rust 管线返回并接入真实本地照片路径。
- **首个 Canvas 预览模板接入**：`PreviewPane` 开始渲染 `classic-bottom` 预览，不再只显示占位框。
- **单张导出接入真实合成**：导出动作开始生成带 `classic-bottom` 底栏的新图，不再只是占位按钮。
- **参数区显示只读 EXIF**：选中照片后可在面板底部查看机身、镜头、曝光参数、时间与 GPS（若存在）。
- **HEIC 导入回退解码**：在 macOS 上优先使用系统能力将 HEIC 转换后接入现有 Rust 管线；若系统解码失败则返回明确错误。

### UI / 风格
- **预览画布外阴影柔化**：中间预览区的画布容器阴影从较重的通用 elevated shadow 调整为更轻、更散的冷灰阴影，减少白色画布底边发脏和阴影层级不自然的问题。
- **移除预览舞台背景层**：中间预览区不再额外绘制灰白舞台底，只保留在下沉区域内的 `3:2` 约束与居中展示；白色画布不会再叠在一层灰底之上，窗口伸缩时仍保持稳定的预览边界。
- **预览舞台边距回调**：中间预览区从过满状态回收一档，外边距由 `p-3` 调整为 `p-4`，让画布与下沉边界保持更稳定的留白关系。
- **预览舞台进一步放大填充下沉区域**：预览区外边距从 `p-6` 收紧到 `p-3`，`3:2` 舞台改为优先吃满可用高度并按比例回推宽度，让下沉区域尽量被预览填满，仅保留一圈小间距。
- **预览舞台统一为固定 `3:2`**：空态、加载态、错误态与已生成预览统一落在 `3:2` 显示区域中，收紧预览区几何边界；不同输出比例会在舞台内保留更多留白，但不影响导出结果。
- **预览未就绪时改为过渡动画**：选中照片但预览尚未加载完成时，预览区展示 `3:2` 过渡骨架，不再只显示静态文案，降低点击后的卡顿感。
- **预览区增加固定展示比例**：预览画布容器改为固定 `3:2` 比例，作为统一“画布舞台”展示区域，避免不同照片纵横比导致预览区跳变。
- **边距参数改为画布百分比**：布局区四边边距从绝对数值改为 `%`（支持 0.5 步进），预览与导出统一按画布宽度比例换算，减少不同尺寸照片的失衡感。
- **经典底栏模板视觉对齐示例风格**：默认关闭 Logo/镜头/时间/GPS，仅保留“机身 + 参数”两行居中底文；主图圆角与阴影参数调整为更接近参考样式。
- **图片列表改为名称块视图**：右侧照片列表移除缩略图网格，改为文件名条目块（含尺寸信息）；保留选中高亮与现有 neu 边框体系，降低导入期绘制与解码压力。
- **主区与底部画廊边界对齐**：三栏主工作区统一收到底边，参数区 / 预览区 / 图片列表与底部模板区共享同一条分界线。
- **预览区与图片列表底边对齐**：中间预览外层边距收紧到与右侧列表一致，下凹内容区底边落在同一水平线上。
- **印框生成逻辑修正**：`classic-bottom` 不再通过压缩主图给信息栏腾空间，而是扩展外部画布形成真实底部边框；Canvas 预览与 Rust 导出统一为同一边框概念。
- **边框几何细化**：底部区域改为“信息栏高度 + 额外底边距”叠加模型，主图、信息栏、留白三者语义分离，预览/导出一致。
- **初始窗口尺寸调整**：桌面端默认窗口从 `800×600` 提升到 `1440×900`，更贴合当前三栏 + 底部画廊布局。
- **初始窗口默认居中**：桌面端启动时将主窗口居中显示，避免首次打开偏向左上角。
- **新增 `ResizeHandle` 可拖拽分隔条**：照片列表与预览之间替换静态 `divider-v` 为拖拽条（4px 命中区 + hover 主色高亮 + 全局 `cursor: col-resize`）；中心带三点药丸拖拽把手（neu-raised 底 + hover 抬升换主色），暗示可拖拽；列表宽度 clamp 200-400px，持久化到 `localStorage`（key `painting-box-layout`），默认 240px。
- **风格文档升级至 v1.3**：废弃 `.label-raised`（浮块过于抢戏），改为三层标签体系（分组标题 `.label-inset` → 子类标题 `.label-plain` → Slider hint）。
- **Chip 统一长款药丸形**：`h-[22px] min-w-[36px] rounded-full px-2.5`，去除 `.chip-icon` 方形变体；联动锁等单图标开关一并统一。
- **Num-input 单位外置**：`%` `px` 等从输入框内 overlay 改为并排独立 `<span>`（`gap-1`，10px muted/70），避免视觉干扰输入区。
- **数值样式统一**：全项 `tabular-nums`；计数徽标一律 `text-[10px] text-muted-foreground/70`。
- **预设/照片列表空态极简化**：移除装饰图标与引导文案，只保留必要按钮（如"保存当前参数"）。
- 参数面板字体 Section 顺序调整：字重 → 字号 → 字距 → 行高 → 颜色 → 对齐。
- `.surface-inset` 启用 `backdrop-filter: blur(24px) saturate(1.4)`，下凹内容区呈"玻璃槽"质感；工具区保持扁平。

### 架构
- **模板切换改为加载模板基础配置**：`template-store` 新增按 `TemplateKind` 管理的基础配置表；切换模板时会回到该模板的基础 `config/frameParams`，`reset` 也按当前模板基线生效，预设仍保持独立快照机制。
- **新增模板级水印位置策略层**：引入 `src/lib/watermark/template-layout.ts`，由模板 kind 统一返回水印位置策略；经典底栏先落地为 `center`，为后续模板库按模板扩展位置规则预留接入点。
- 新增 Phase 2 导入/导出契约类型与 `src/lib/tauri/photos.ts` 包装层，前端开始通过统一 Tauri 命令接口接入真实照片管线。
- 补齐 Tauri capability `dialog:default`，允许桌面端调用系统文件选择器。
- 新增 Tauri 命令 `load_photo_exif`：将导入元信息与 EXIF 读取拆分为两条链路，降低批量导入首屏阻塞。
- EXIF 解析管线去掉全量 `raw` tag 遍历，改为仅提取展示/导出所需核心字段，减少单张元数据解析耗时。
- 模板文本格式化新增机身去重逻辑（避免 `Make + Model` 重复品牌名），并统一参数分隔符为单空格风格。
- Rust 导出补齐模板关键几何层：主图圆角裁切与软阴影绘制接入 `frameParams`（`innerRadius`/`shadow*`），减少预览与导出观感偏差。
- 新增 `src/stores/preset-store.ts`：zustand `persist` 中间件，localStorage 持久化预设（key `painting-box-presets`）。
- 新增 `src/lib/templates.ts` + `TEMPLATE_LIBRARY`：11 套模板枚举（classic-bottom / polaroid / minimal-corner / magazine / film-strip / full-frame / leica / poster / square-social / xpan / minimal-blank）。
- 新增 `src/components/gallery/template-gallery.tsx` / `preset-gallery.tsx`：底部双横向滚动画廊（flex `3:2`），支持 inline 命名、双击重命名、选中删除。
- 参数面板扩展 `FrameParams` 至 30+ 字段，新增 8 个可折叠 Section：布局 / 背景 / 阴影 / 照片 / 文字 / Logo / 分隔线 / 显示项。
- 三栏骨架：`FrameParamsPanel (w-72) | PreviewPane (flex-1) | PhotoList (w-52)`，栏间 `.divider-v` 分隔。

---

## 使用约定

- **何时追加条目**：任一用户可感知的变更（UI、交互、数据结构、持久化、快捷键）。纯内部重构、格式化不记录。
- **分类**：`UI / 风格`、`架构`、`功能`、`修复`、`性能`、`文档`。按需裁剪。
- **格式**：用命令式现在时 + 结果（做了什么 / 为什么值得知道），而不是提交日志的流水账。
- **发版**：`master` 切 `v0.x.0` tag 时，把 `[Unreleased]` 改为版本号 + 日期，并在上面新开 `[Unreleased]` 块。
- 2026-04-20
  - 模板库暂时隐藏未实现模板：模板画廊当前只保留 `classic-bottom / classic-white / polaroid / minimal-corner`，避免选到未完成的占位模板。
  - `classic-white` 默认显示项改为仅显示 Logo；同时修复“只显示 Logo 时无法渲染水印”的问题，前后端预览/导出都支持 logo-only 水印。
  - `minimal-corner` 改为在图片下方的水印区域右对齐显示，不再覆盖在照片上；同时从模板库中移除 `polaroid`。
  - 新增模板能力边界：`classic-white` 固定为 logo-only，显示项面板只保留 Logo，旧预设或手动配置也不能打开机身/镜头/参数；`classic-white` 的 logo-only 水印默认上移一行。
  - 预设选中态改为严格匹配：应用预设后保持选中；之后只要修改模板、参数、显示项或重置参数，就会自动取消当前预设选中。
  - `minimal-corner` 也收紧为 logo-only 模板，默认只显示 Logo，显示项面板只保留 Logo，旧预设不能打开其他水印字段。
  - `minimal-corner` 的 logo-only 水印位置与 `classic-white` 对齐，默认在水印区域内上移一行，预览和导出保持一致。
  - 导出入口暂时收紧为“导出当前照片”：必须先选中照片，弹窗直接选择导出格式/质量并保存单张结果，移除未实现的批量导出文案和输出目录占位。
  - 导出入口扩展为照片维度和列表维度：每张照片条目新增单张导出按钮，顶部导出按钮改为导出整个列表，并支持当前会话内已导出照片的跳过/覆盖/重命名策略；默认导出后缀改为 `_painting_box`。
  - 照片占比范围扩展并约束为 `70-95%`，模板默认值、旧预设和旧持久化参数都会被限制在可配置范围内。
  - 修复 UI 导出失败：前端调用 Tauri 导出命令时会把导出质量写入 `frameParams.exportQuality`，匹配 Rust 端反序列化结构。
  - 修复 `minimal-corner` 预览和导出水印位置不一致：Rust 导出端同步改为底部水印区右对齐，而不是覆盖在照片上。
  - 导出性能优化第一步：Rust 导出端缓存字体和 SVG Logo 位图，避免每张照片重复读取字体文件和解析 Logo。
